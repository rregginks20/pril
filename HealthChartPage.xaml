<Page x:Class="YanaulKioskPerfect.HealthChartPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
      Title="Графики здоровья" Background="#F5F9FC">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
            <Button Content="📈 Давление" Click="BtnPressure_Click" Margin="0,0,10,0" Padding="20,10" Background="#3498DB" Foreground="White" BorderThickness="0" Cursor="Hand"/>
            <Button Content="⚖️ Вес" Click="BtnWeight_Click" Padding="20,10" Background="#2ECC71" Foreground="White" BorderThickness="0" Cursor="Hand"/>
            <Button Content="← Назад" Click="BtnBack_Click" Margin="20,0,0,0" Padding="20,10" Background="#95A5A6" Foreground="White" BorderThickness="0" Cursor="Hand"/>
        </StackPanel>
        <Border Grid.Row="1" Background="White" CornerRadius="15" Padding="20" BorderBrush="#DCE4EC" BorderThickness="1">
            <Grid>
                <TextBlock x:Name="LblNoData" Text="Нет данных для отображения" FontSize="20" Foreground="Gray" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>
                <lvc:CartesianChart x:Name="MainChart" LegendLocation="Right" Margin="10">
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis Title="Дата" Labels="{Binding DatesLabels}" FontSize="12"/>
                    </lvc:CartesianChart.AxisX>
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis Title="Значение" FontSize="12"/>
                    </lvc:CartesianChart.AxisY>
                </lvc:CartesianChart>
            </Grid>
        </Border>
    </Grid>
</Page>